{{#if cancel}}
  {{form-submission-button
    action=cancelAction
    disabled=disabled
    text=cancelText
    type='reset'
  }}
{{/if}}

{{#if delete}}
  {{form-submission-button
    action=deleteAction
    disabled=disabled
    text=deleteText
  }}
{{/if}}

{{#if save}}
  {{form-submission-button
    action=saveAction
    disabled=disabled
    text=saveText
    type='submit'
  }}
{{/if}}
